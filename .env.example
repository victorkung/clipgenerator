# Copy to .env and fill in your own key. Never commit .env.
# Get a key at https://console.x.ai/
#
# Required only when transcription uses xAI STT (no sidecar .vtt/.srt captions).
# YouTube with --with-subs can often skip STT entirely.

XAI_API_KEY=
